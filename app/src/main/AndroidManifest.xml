<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Camera permission -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Hardware feature declaration -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <application>
        <!-- Scanner Activity -->
        <activity
            android:name=".presentation.ScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <!-- FileProvider is declared once in :app's AndroidManifest.xml with the
             same "${applicationId}.fileprovider" authority (see app/src/main/res/xml/file_paths.xml,
             which now includes the scanner_original/processed/thumbnails paths too).
             Declaring it again here caused a manifest-merger conflict (duplicate
             provider with differing meta-data resource). -->
    </application>
</manifest>
